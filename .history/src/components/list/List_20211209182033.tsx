import React, {FC} from 'react'

type UserType = {
    user: string,
    userId: number,
}

const List: FC<UserType> = ({user}) => {
    return (
        <ul>
            
        </ul>
    )
}

export default List